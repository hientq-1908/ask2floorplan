# retriever — v1

TODO: system prompt.

Prompts are versioned by directory (v1/, v2/, ...) so a training run can
record which prompt produced its data. Never edit a version in place once
trajectories have been generated from it.
