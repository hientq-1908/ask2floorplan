Exported JSON Schema lands here, one directory per SCHEMA_VERSION.

Generated by `uv run python -m a2fp_contract.export_schema` and committed.
Drives both pydantic validation and the agent constrained decoding, so the
two cannot silently desync. CI fails on drift.
